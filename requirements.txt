# Создаёт файл requirements.txt для данного скрипта с рекомендуемыми
# зависимостями.
requirements = [
    "# Основные библиотеки",
    "numpy>=1.21",
    "pandas>=1.3",
    "openpyxl>=3.0.10  # для чтения/записи Excel (xlsx)",
    "xlrd>=2.0.1       # для чтения старых xls (опционально)",
    "",
    "# Веб-интерфейс (опционально, если будете запускать Streamlit)",
    "streamlit>=1.10",
    "",
    "# Для морфологии (опционально, если нужен pymorphy2)",
    "pymorphy2>=0.9.1",
    "pymorphy2-dicts-ru>=2.4",
    "",
    "# Примечание: дополнительные пакеты могут потребоваться в зависимости от окружения."
]

txt = "\n".join(requirements)
with open("requirements.txt", "w", encoding="utf-8") as f:
    f.write(txt)

print("requirements.txt создан. Содержимое:\n")
print(txt)
