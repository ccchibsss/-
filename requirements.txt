 Создаёт файл requirements.txt для улучшенного Streamlit-приложения
requirements = [
    "streamlit>=1.20.0",
    "pandas>=1.5.3",
    "numpy>=1.24.0",
    "openpyxl>=3.0.10",
    "pymorphy2>=0.9.1",
]

filename = "requirements.txt"

with open(filename, "w", encoding="utf-8") as f:
    f.write("\n".join(requirements) + "\n")

print(f"Created {filename} with {len(requirements)} entries:")
print("\n".join(requirements))
