# Обязательные зависимости
pandas>=1.5.0
requests>=2.28.0
numpy>=1.23.0
openpyxl>=3.0.0    # для чтения/записи .xlsx
xlrd>=2.0.1        # чтение старых .xls (опционально)

# Опционально (если используете UI / улучшенные графики / морфологию)
# Установите только те, которые вам нужны:
streamlit>=1.20.0   # веб-интерфейс (если используете streamlit)
altair>=4.2.0       # более красивые графики в streamlit (опционально)
pymorphy2>=0.9.1    # склонение русских слов (опционально)

# Пример: если хотите ускорить fuzzy-совпадения, можно добавить:
# python-Levenshtein>=0.20.9
